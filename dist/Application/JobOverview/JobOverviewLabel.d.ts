import * as React from 'react';
declare const JobOverviewLabel: React.FunctionComponent<Props>;
interface Props {
    children: React.ReactNode;
    style?: React.CSSProperties;
}
export default JobOverviewLabel;
